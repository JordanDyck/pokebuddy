const title = () => {
  return (
    <div className="top-banner">
      <header id="title" onClick={() => window.location.reload(false)}>
        PokéBuddy
      </header>
    </div>
  )
}
export default title
